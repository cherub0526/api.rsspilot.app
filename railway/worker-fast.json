{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "docker/Dockerfile.railway"
  },
  "deploy": {
    "startCommand": "php /var/www/artisan queue:work database --queue='media.info,media.caption,media.youtube-data-caption,videotranscriber.start,videotranscriber.fetch' --sleep=3 --timeout=120 --max-time=3600 --memory=256",
    "restartPolicyType": "ALWAYS",
    "numReplicas": 1
  }
}
