{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "docker/Dockerfile.railway"
  },
  "deploy": {
    "startCommand": "php /var/www/artisan schedule:run",
    "cronSchedule": "* * * * *",
    "restartPolicyType": "NEVER"
  }
}
